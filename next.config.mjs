/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "nnatyrhyhd6ziusq.public.blob.vercel-storage.com"
      }
    ]
  }
};

export default nextConfig;
